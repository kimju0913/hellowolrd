<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.helloworld.MainMapper">
 	<select id = "select_all_member" resultType="MemberVO">
 		select * from ${member} where ${col_userid} not in(#{userid})
 	</select>
	
	
	
</mapper>